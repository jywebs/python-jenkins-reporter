# python-jenkins-reporter
Generates a CSV ranking Jenkins jobs by build count and runtime across one or more controllers.   Uses `python-jenkins` (official library) and Basic Auth with username + API token.
